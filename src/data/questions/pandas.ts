import type { Question } from "@/types";

export const pandasQuestions: Question[] = [
  {
    id: "pd-001",
    type: "multiple-choice",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["dataframe"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["DataFrame", "Series"],
    prompt: "Que retourne `df[\"colonne\"]` sur un DataFrame ?",
    courseId: "pandas-series-dataframe",
    hints: ["Ce n'est pas un DataFrame à une seule colonne, mais un type d'objet différent."],
    explanation:
      "Sélectionner une seule colonne avec `df[\"colonne\"]` retourne une `Series` (structure 1D indexée), pas un DataFrame. Pour obtenir un DataFrame à une seule colonne, il faut utiliser une liste : `df[[\"colonne\"]]`.",
    options: [
      { id: "a", text: "Une Series", correct: true },
      { id: "b", text: "Un DataFrame à une colonne", correct: false, whyWrong: "Il faudrait df[[\"colonne\"]] (avec double crochets) pour obtenir un DataFrame." },
      { id: "c", text: "Une liste Python", correct: false, whyWrong: "Le résultat reste un objet Pandas (Series), pas une liste native Python." },
      { id: "d", text: "Un tableau NumPy", correct: false, whyWrong: "C'est une Series Pandas, qui encapsule un tableau NumPy mais y ajoute un index." },
    ],
  },
  {
    id: "pd-002",
    type: "multiple-choice",
    kind: "code-analysis",
    level: 5,
    topic: "pandas",
    subtopics: ["loc-iloc"],
    difficulty: 4,
    cognitiveLevel: "analyse",
    tags: ["loc", "iloc"],
    prompt:
      "Pour un DataFrame `df` avec un index par défaut `0,1,2,3,4`, combien de lignes retourne `df.loc[0:2]` ?",
    courseId: "pandas-selection",
    hints: ["`loc` inclut la borne de FIN, contrairement au slicing Python classique."],
    explanation:
      "`loc` effectue une sélection par **étiquette**, où la borne de fin est **incluse** (contrairement à `iloc` ou au slicing Python classique) : `df.loc[0:2]` retourne les lignes d'étiquette 0, 1 ET 2, soit 3 lignes.",
    options: [
      { id: "a", text: "3 lignes (0, 1 et 2)", correct: true },
      { id: "b", text: "2 lignes (0 et 1)", correct: false, whyWrong: "Ce serait le comportement de iloc[0:2], pas de loc[0:2]." },
      { id: "c", text: "5 lignes, toutes", correct: false, whyWrong: "loc[0:2] se limite bien aux étiquettes 0, 1 et 2, pas à tout le DataFrame." },
      { id: "d", text: "Une erreur, loc ne supporte pas le slicing", correct: false, whyWrong: "loc supporte parfaitement le slicing par étiquette, avec borne de fin incluse." },
    ],
  },
  {
    id: "pd-003",
    type: "true-false",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["boolean-indexing"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["boolean-indexing"],
    prompt: "Pour combiner deux conditions dans un filtre Pandas, on utilise `and`/`or` comme en Python classique.",
    courseId: "pandas-selection",
    hints: ["`and`/`or` attendent des booléens SCALAIRES uniques.", "Un filtre produit un tableau de booléens, pas un seul booléen."],
    explanation:
      "Faux : `and`/`or` ne fonctionnent que sur des booléens scalaires uniques et lèveraient une erreur (`ValueError: truth value of a Series is ambiguous`) sur un tableau de booléens. Il faut utiliser les opérateurs bit à bit `&`, `|`, `~`, en parenthésant chaque condition : `df[(df[\"a\"] > 0) & (df[\"b\"] < 10)]`.",
    correct: false,
  },
  {
    id: "pd-004",
    type: "multiple-choice",
    kind: "realistic",
    level: 5,
    topic: "pandas",
    subtopics: ["groupby"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["groupby", "realistic"],
    prompt: "Quelle expression calcule la moyenne du salaire PAR département, à partir d'un DataFrame `df` avec les colonnes `department` et `salary` ?",
    courseId: "pandas-groupby",
    hints: ["`groupby` suivi d'une sélection de colonne, puis d'une agrégation."],
    explanation:
      "`df.groupby(\"department\")[\"salary\"].mean()` regroupe les lignes par département, sélectionne la colonne `salary` pour chaque groupe, puis calcule la moyenne — retournant une Series indexée par département.",
    options: [
      { id: "a", text: 'df.groupby("department")["salary"].mean()', correct: true },
      { id: "b", text: 'df.mean("department", "salary")', correct: false, whyWrong: "DataFrame.mean() n'accepte pas d'arguments de colonnes de cette façon ; ce n'est pas la syntaxe de groupby." },
      { id: "c", text: 'df["salary"].groupby().mean("department")', correct: false, whyWrong: "groupby() nécessite la clé de regroupement en argument, pas mean()." },
      { id: "d", text: 'df.filter("department").mean()', correct: false, whyWrong: "filter() sélectionne des colonnes/lignes par nom/condition, ce n'est pas un mécanisme de regroupement." },
    ],
  },
  {
    id: "pd-005",
    type: "multiple-choice",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["merge"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["merge", "how"],
    prompt: "Dans `pd.merge(a, b, on=\"id\", how=\"left\")`, que se passe-t-il pour une ligne de `a` sans correspondance dans `b` ?",
    courseId: "pandas-merge-join-concat",
    hints: ["`how=\"left\"` garantit la conservation de TOUTES les lignes de la table de GAUCHE."],
    explanation:
      "Avec `how=\"left\"`, toutes les lignes de `a` (la table de gauche) sont conservées, même sans correspondance dans `b` — les colonnes provenant de `b` sont alors remplies avec `NaN` pour cette ligne, plutôt que de la faire disparaître du résultat.",
    options: [
      { id: "a", text: "Elle est conservée, avec des NaN pour les colonnes de b", correct: true },
      { id: "b", text: "Elle est supprimée du résultat", correct: false, whyWrong: "Ce serait le comportement d'un how=\"inner\", pas how=\"left\"." },
      { id: "c", text: "Une exception est levée", correct: false, whyWrong: "merge ne lève pas d'exception pour des lignes sans correspondance ; il produit simplement des NaN." },
      { id: "d", text: "Elle est dupliquée pour chaque ligne de b", correct: false, whyWrong: "Cela n'arrive que s'il existe plusieurs correspondances, pas en l'absence de correspondance." },
    ],
  },
  {
    id: "pd-006",
    type: "true-false",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["missing-values"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["NaN", "dtype"],
    prompt: "Insérer un seul `NaN` dans une colonne d'entiers (dtype int64) la convertit automatiquement en float64.",
    courseId: "pandas-missing-cleaning",
    hints: ["`NaN` est en réalité une valeur de type float en Python/NumPy."],
    explanation:
      "Vrai : `NaN` est historiquement une valeur de type `float` (il n'existe pas de \"NaN entier\" natif en NumPy classique). Une colonne d'entiers contenant un `NaN` doit donc changer de dtype pour `float64`, ce qui convertit aussi tous les autres entiers de la colonne en flottants (`1` devient `1.0`).",
    correct: true,
  },
  {
    id: "pd-007",
    type: "code-editor",
    kind: "write-code",
    level: 5,
    topic: "pandas",
    subtopics: ["groupby", "realistic"],
    difficulty: 4,
    cognitiveLevel: "application",
    tags: ["groupby", "realistic", "dict"],
    prompt:
      "Écris une fonction `salaire_moyen_par_departement(donnees)` qui reçoit une LISTE de dicts `{\"departement\": str, \"salaire\": float}` et retourne un DICT `{departement: salaire_moyen}` (arrondi à 2 décimales), en utilisant Pandas.",
    courseId: "pandas-groupby",
    hints: [
      "Convertis la liste de dicts en DataFrame avec `pd.DataFrame(donnees)`.",
      "Utilise `.groupby(\"departement\")[\"salaire\"].mean()`.",
      "`.round(2).to_dict()` convertit le résultat final en dictionnaire Python.",
    ],
    explanation:
      "`pd.DataFrame(donnees)` construit un DataFrame à partir de la liste de dicts. `groupby(\"departement\")[\"salaire\"].mean()` calcule la moyenne par groupe. `.round(2)` arrondit, `.to_dict()` reconvertit la Series résultante en dict Python natif.",
    starterCode:
      "import pandas as pd\n\ndef salaire_moyen_par_departement(donnees):\n    # Ton code ici\n    pass\n",
    solution:
      "import pandas as pd\n\ndef salaire_moyen_par_departement(donnees):\n    df = pd.DataFrame(donnees)\n    resultat = df.groupby(\"departement\")[\"salaire\"].mean().round(2)\n    return resultat.to_dict()\n",
    testCases: [
      {
        call:
          'salaire_moyen_par_departement([{"departement": "IT", "salaire": 3000}, {"departement": "IT", "salaire": 5000}, {"departement": "RH", "salaire": 4000}])',
        expected: { IT: 4000.0, RH: 4000.0 },
        description: "Moyenne correcte par groupe",
      },
    ],
  },
  {
    id: "pd-008",
    type: "multiple-choice",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["performance"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["apply", "vectorization"],
    prompt: "Pourquoi `df[\"col\"].apply(fonction)` est-il généralement plus LENT que `df[\"col\"] * 2` ?",
    courseId: "pandas-missing-cleaning",
    hints: ["`apply` exécute une fonction Python arbitraire, ligne par ligne.", "Une opération native comme `*` est implémentée en C, vectorisée."],
    explanation:
      "`apply` appelle une fonction Python arbitraire pour **chaque ligne** individuellement, en repassant à chaque fois par l'interpréteur Python — un coût significatif à grande échelle. Une opération native vectorisée (`* 2`, `.str.upper()`, etc.) est exécutée entièrement en code C optimisé sur tout le tableau d'un coup, sans repasser par l'interpréteur pour chaque élément.",
    options: [
      { id: "a", text: "apply repasse par l'interpréteur Python à chaque ligne, contrairement aux opérations vectorisées en C", correct: true },
      { id: "b", text: "apply ne fonctionne que sur des DataFrames, jamais sur des Series", correct: false, whyWrong: "apply fonctionne parfaitement sur une Series comme sur un DataFrame." },
      { id: "c", text: "apply recharge tout le fichier CSV à chaque appel", correct: false, whyWrong: "apply opère sur des données déjà en mémoire, sans aucun rechargement de fichier." },
      { id: "d", text: "* 2 ne fonctionne qu'avec des entiers", correct: false, whyWrong: "L'opérateur * fonctionne aussi avec des floats et d'autres types numériques compatibles." },
    ],
  },
  {
    id: "pd-009",
    type: "code-output",
    kind: "code-analysis",
    level: 5,
    topic: "pandas",
    subtopics: ["dropna"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["dropna", "realistic"],
    prompt: "Combien de lignes retourne `df.dropna(subset=[\"age\"])` pour ce DataFrame ?",
    code: 'df = pd.DataFrame({"age": [25, None, 31, None, 40]})\ndf.dropna(subset=["age"])',
    courseId: "pandas-missing-cleaning",
    hints: ["`subset` restreint la vérification de valeurs manquantes à certaines colonnes seulement."],
    explanation:
      "`dropna(subset=[\"age\"])` supprime uniquement les lignes ayant un `NaN` dans la colonne `\"age\"` spécifiquement (les NaN dans d'autres colonnes, s'il y en avait, seraient ignorés). Avec 2 lignes concernées sur 5, il en reste `3`.",
    expectedOutput: "3",
  },
  {
    id: "pd-010",
    type: "multiple-choice",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["pivot"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["pivot_table", "pivot"],
    prompt: "Quelle est la différence principale entre `pivot` et `pivot_table` ?",
    courseId: "pandas-merge-join-concat",
    hints: ["L'un des deux gère les doublons en les agrégeant automatiquement."],
    explanation:
      "`pivot` restructure les données sans aucune agrégation et lève une erreur en cas de doublons de combinaison ligne/colonne. `pivot_table` gère automatiquement les doublons en appliquant une fonction d'agrégation (`aggfunc`, par défaut la moyenne), rendant son usage plus robuste sur des données réelles.",
    options: [
      { id: "a", text: "pivot_table agrège automatiquement les doublons, pivot lève une erreur", correct: true },
      { id: "b", text: "pivot est plus récent et remplace pivot_table", correct: false, whyWrong: "Les deux méthodes coexistent, avec des cas d'usage différents ; ni ne remplace l'autre." },
      { id: "c", text: "pivot_table ne fonctionne qu'avec des données numériques", correct: false, whyWrong: "pivot_table peut aussi agréger avec des fonctions comme 'count' sur des données non numériques." },
      { id: "d", text: "Ce sont des alias strictement identiques", correct: false, whyWrong: "Leur comportement diffère bien concrètement face aux doublons." },
    ],
  },
  {
    id: "pd-011",
    type: "true-false",
    kind: "knowledge",
    level: 5,
    topic: "pandas",
    subtopics: ["csv"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["read_csv", "to_csv"],
    prompt: "`df.to_csv(\"out.csv\", index=False)` évite d'écrire une colonne d'index supplémentaire dans le fichier.",
    courseId: "pandas-io",
    hints: ["Par défaut, to_csv écrit l'index du DataFrame comme première colonne."],
    explanation:
      "Vrai : par défaut, `to_csv` écrit l'index du DataFrame comme colonne supplémentaire en tête de fichier. `index=False` désactive cela, ce qui est généralement souhaité quand l'index est juste une numérotation automatique sans signification métier.",
    correct: true,
  },
  {
    id: "pd-012",
    type: "code-output",
    kind: "prediction",
    level: 5,
    topic: "pandas",
    subtopics: ["indexing"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["iloc", "slicing"],
    prompt: "Combien de lignes retourne `df.iloc[0:2]` pour ce DataFrame de 5 lignes (index par défaut 0 à 4) ?",
    code: "df = pd.DataFrame({\"x\": [10, 20, 30, 40, 50]})\ndf.iloc[0:2]",
    courseId: "pandas-selection",
    hints: ["iloc suit la convention Python classique : borne de fin EXCLUE."],
    explanation:
      "`iloc` sélectionne par position, avec une borne de fin **exclue**, comme le slicing Python classique. `df.iloc[0:2]` retourne donc les positions 0 et 1 seulement : 2 lignes.",
    expectedOutput: "2",
  },
  {
    id: "pd-013",
    type: "multiple-choice",
    kind: "realistic",
    level: 5,
    topic: "pandas",
    subtopics: ["fillna", "realistic"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["fillna"],
    prompt: "Quelle expression remplace les valeurs manquantes de la colonne `\"age\"` par la MÉDIANE de cette même colonne ?",
    courseId: "pandas-missing-cleaning",
    hints: ["`fillna` accepte une valeur scalaire à utiliser en remplacement.", "`.median()` calcule la médiane d'une Series."],
    explanation:
      "`df[\"age\"] = df[\"age\"].fillna(df[\"age\"].median())` calcule d'abord la médiane de la colonne (en ignorant automatiquement les NaN existants), puis remplace chaque valeur manquante par cette médiane — une stratégie d'imputation courante, plus robuste aux valeurs extrêmes que la moyenne.",
    options: [
      { id: "a", text: 'df["age"] = df["age"].fillna(df["age"].median())', correct: true },
      { id: "b", text: 'df["age"] = df["age"].dropna(df["age"].median())', correct: false, whyWrong: "dropna() supprime des lignes, il ne prend pas de valeur de remplacement en argument." },
      { id: "c", text: 'df["age"].median() = df["age"].fillna()', correct: false, whyWrong: "On ne peut pas affecter une valeur à un appel de méthode ; cette syntaxe est invalide." },
      { id: "d", text: 'df["age"] = df["age"].isna(df["age"].median())', correct: false, whyWrong: "isna() détecte les valeurs manquantes (retourne des booléens), elle ne les remplace pas." },
    ],
  },
  {
    id: "pd-014",
    type: "code-editor",
    kind: "write-code",
    level: 5,
    topic: "pandas",
    subtopics: ["filtering", "realistic"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["boolean-indexing", "realistic"],
    prompt:
      "Écris une fonction `noms_actifs_tries(donnees)` qui reçoit une LISTE de dicts `{\"nom\": str, \"actif\": bool}` et retourne, via Pandas, la liste des noms des utilisateurs actifs, triés alphabétiquement.",
    courseId: "pandas-selection",
    hints: [
      "Construis un DataFrame avec `pd.DataFrame(donnees)`.",
      "Filtre avec `df[df[\"actif\"]]`.",
      "Trie avec `.sort_values(\"nom\")` puis extrait la colonne en liste avec `.tolist()`.",
    ],
    explanation:
      "On filtre les lignes où `actif` est `True` via boolean indexing, on trie par nom avec `sort_values`, puis on extrait la colonne `\"nom\"` sous forme de liste Python avec `.tolist()`.",
    starterCode: "import pandas as pd\n\ndef noms_actifs_tries(donnees):\n    # Ton code ici\n    pass\n",
    solution:
      "import pandas as pd\n\ndef noms_actifs_tries(donnees):\n    df = pd.DataFrame(donnees)\n    actifs = df[df[\"actif\"]].sort_values(\"nom\")\n    return actifs[\"nom\"].tolist()\n",
    testCases: [
      {
        call:
          'noms_actifs_tries([{"nom": "Zoe", "actif": True}, {"nom": "Bob", "actif": False}, {"nom": "Ada", "actif": True}])',
        expected: ["Ada", "Zoe"],
        description: "Filtre les inactifs et trie alphabétiquement",
      },
    ],
  },
];
