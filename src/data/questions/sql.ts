import type { Question } from "@/types";

export const sqlQuestions: Question[] = [
  {
    id: "sql-001",
    type: "multiple-choice",
    kind: "find-error",
    level: 3,
    topic: "sql",
    subtopics: ["injection"],
    difficulty: 4,
    cognitiveLevel: "piege",
    tags: ["injection", "security"],
    prompt:
      "Quel est le problème de sécurité dans ce code ?\n\n```python\nnom = input(\"Nom : \")\ncur.execute(f\"SELECT * FROM users WHERE nom = '{nom}'\")\n```",
    courseId: "sql-injections",
    hints: ["Que se passe-t-il si l'utilisateur entre `' OR '1'='1` comme nom ?", "La valeur utilisateur est directement injectée dans le texte de la requête SQL."],
    explanation:
      "C'est une **injection SQL** classique : la valeur de `nom` est directement concaténée dans le texte de la requête via f-string. Un utilisateur malveillant entrant `' OR '1'='1` transformerait la requête en une condition toujours vraie, exposant potentiellement toutes les lignes de la table. La correction consiste à utiliser une requête paramétrée : `cur.execute(\"SELECT * FROM users WHERE nom = ?\", (nom,))`.",
    options: [
      { id: "a", text: "Injection SQL : la valeur utilisateur est concaténée directement dans la requête", correct: true },
      { id: "b", text: "execute() ne peut pas contenir de SELECT", correct: false, whyWrong: "execute() peut exécuter n'importe quelle instruction SQL valide, y compris SELECT." },
      { id: "c", text: "Il manque un appel à conn.commit()", correct: false, whyWrong: "commit() n'est nécessaire que pour les écritures (INSERT/UPDATE/DELETE), pas pour un SELECT en lecture." },
      { id: "d", text: "f-string n'est pas compatible avec sqlite3", correct: false, whyWrong: "Le problème n'est pas technique (f-string fonctionne très bien), mais un problème de sécurité (permet l'injection)." },
    ],
  },
  {
    id: "sql-002",
    type: "text",
    kind: "fix-code",
    level: 3,
    topic: "sql",
    subtopics: ["injection"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["parameterized-queries"],
    prompt: "Corrige cette requête sqlite3 vulnérable en une version paramétrée sûre : `cur.execute(f\"SELECT * FROM users WHERE nom = '{nom}'\")`",
    courseId: "sql-injections",
    hints: ["sqlite3 utilise `?` comme placeholder de paramètre.", "Les paramètres sont passés en second argument, sous forme de tuple."],
    explanation:
      "`cur.execute(\"SELECT * FROM users WHERE nom = ?\", (nom,))` utilise un placeholder `?` que le driver remplace de façon sûre — la valeur ne peut jamais être interprétée comme du SQL exécutable, quelle que soit son contenu.",
    acceptedAnswers: [
      'cur.execute("SELECT * FROM users WHERE nom = ?", (nom,))',
      "cur.execute('SELECT * FROM users WHERE nom = ?', (nom,))",
    ],
    caseSensitive: false,
  },
  {
    id: "sql-003",
    type: "multiple-choice",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["transactions"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["transactions", "commit"],
    prompt: "Que se passe-t-il si on ne fait jamais `conn.commit()` après des `INSERT` avec sqlite3 ?",
    courseId: "sql-sqlite3-basics",
    hints: ["Pense à la persistance des changements dans le fichier de base de données."],
    explanation:
      "Sans `commit()`, les modifications restent dans une transaction non validée : elles ne sont **jamais persistées** dans le fichier de base de données. Si la connexion se ferme sans commit (ou en cas de rollback explicite ou implicite), ces changements sont perdus.",
    options: [
      { id: "a", text: "Les modifications ne sont jamais persistées dans le fichier de base de données", correct: true },
      { id: "b", text: "sqlite3 fait un commit automatique après chaque instruction", correct: false, whyWrong: "sqlite3 ne commit pas automatiquement chaque instruction ; c'est à l'appelant de le faire explicitement (sauf en mode autocommit spécifique)." },
      { id: "c", text: "Une exception est levée immédiatement", correct: false, whyWrong: "Aucune exception n'est levée pour l'absence de commit ; le programme continue simplement sans persister les changements." },
      { id: "d", text: "Les données sont automatiquement écrites à la fermeture du programme", correct: false, whyWrong: "Sans commit explicite, fermer la connexion peut abandonner les changements non validés." },
    ],
  },
  {
    id: "sql-004",
    type: "multiple-choice",
    kind: "documentation",
    level: 3,
    topic: "sql",
    subtopics: ["joins-groupby"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["SQL", "HAVING"],
    prompt:
      "Cette requête est invalide : `SELECT departement, COUNT(*) FROM employes GROUP BY departement WHERE COUNT(*) > 5`. Quelle clause aurait dû être utilisée à la place de ce `WHERE` ?",
    courseId: "sql-joins-groupby",
    hints: ["WHERE filtre les lignes AVANT le regroupement, pas les résultats d'agrégation.", "Il existe une clause spécifiquement dédiée au filtrage post-agrégation."],
    explanation:
      "`WHERE` ne peut pas référencer le résultat d'une fonction d'agrégation comme `COUNT(*)`, car il s'exécute avant le regroupement. Il faut utiliser `HAVING COUNT(*) > 5` à la place, qui s'applique après le `GROUP BY`, sur les groupes déjà formés.",
    options: [
      { id: "a", text: "HAVING", correct: true },
      { id: "b", text: "GROUP BY", correct: false, whyWrong: "GROUP BY est déjà présent dans la requête ; ce n'est pas lui qui filtre les groupes agrégés." },
      { id: "c", text: "ORDER BY", correct: false, whyWrong: "ORDER BY trie le résultat final, il ne filtre aucune ligne ni aucun groupe." },
      { id: "d", text: "WHERE (la requête est en fait valide)", correct: false, whyWrong: "WHERE ne peut pas référencer une fonction d'agrégation comme COUNT(*) ; la requête est bien invalide telle quelle." },
    ],
  },
  {
    id: "sql-005",
    type: "multiple-choice",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["joins"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["INNER JOIN", "LEFT JOIN"],
    prompt: "Quelle est la différence entre `INNER JOIN` et `LEFT JOIN` ?",
    courseId: "sql-joins-groupby",
    hints: ["L'un des deux garantit de conserver TOUTES les lignes d'une table, même sans correspondance."],
    explanation:
      "`INNER JOIN` ne garde que les lignes ayant une correspondance dans **les deux** tables. `LEFT JOIN` garde toutes les lignes de la table de gauche, avec des `NULL` pour les colonnes de la table de droite quand il n'y a pas de correspondance.",
    options: [
      { id: "a", text: "INNER JOIN exige une correspondance des deux côtés ; LEFT JOIN garde toutes les lignes de gauche", correct: true },
      { id: "b", text: "Ce sont deux syntaxes différentes pour exactement le même résultat", correct: false, whyWrong: "Leur résultat diffère dès qu'il existe des lignes sans correspondance de l'autre côté." },
      { id: "c", text: "LEFT JOIN est plus rapide dans tous les cas", correct: false, whyWrong: "La performance dépend du moteur, des index et des données ; ce n'est pas une règle générale absolue." },
      { id: "d", text: "INNER JOIN ne fonctionne qu'avec des clés numériques", correct: false, whyWrong: "INNER JOIN fonctionne avec n'importe quel type de clé de jointure comparable, y compris du texte." },
    ],
  },
  {
    id: "sql-006",
    type: "code-editor",
    kind: "write-code",
    level: 3,
    topic: "sql",
    subtopics: ["sqlite3"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["sqlite3", "realistic"],
    prompt:
      "Écris une fonction `filtrer_majeurs(cur)` qui reçoit un curseur sqlite3 déjà connecté à une table `users(nom TEXT, age INTEGER)` déjà remplie, et retourne la liste des noms des utilisateurs majeurs (age >= 18), triés alphabétiquement. Utilise une requête PARAMÉTRÉE.",
    courseId: "sql-sqlite3-basics",
    hints: [
      "`cur.execute(\"SELECT nom FROM users WHERE age >= ? ORDER BY nom\", (18,))`.",
      "`cur.fetchall()` retourne une liste de tuples à un élément chacun.",
    ],
    explanation:
      "On exécute une requête paramétrée qui filtre et trie directement en SQL, puis on extrait le premier élément de chaque tuple retourné par `fetchall()` (chaque ligne est un tuple `(nom,)` puisqu'on ne sélectionne qu'une colonne).",
    starterCode:
      "def filtrer_majeurs(cur):\n    # Ton code ici\n    pass\n",
    solution:
      "def filtrer_majeurs(cur):\n    cur.execute(\"SELECT nom FROM users WHERE age >= ? ORDER BY nom\", (18,))\n    return [ligne[0] for ligne in cur.fetchall()]\n",
    testCases: [
      {
        call:
          "(lambda cur: (cur.execute('CREATE TABLE users (nom TEXT, age INTEGER)'), cur.executemany('INSERT INTO users VALUES (?, ?)', [('Bob', 17), ('Alice', 25), ('Zoe', 31)]), filtrer_majeurs(cur))[-1])(__import__('sqlite3').connect(':memory:').cursor())",
        expected: ["Alice", "Zoe"],
        description: "Filtre et trie correctement les utilisateurs majeurs",
      },
    ],
  },
  {
    id: "sql-007",
    type: "true-false",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["fetch"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["fetchall", "fetchone"],
    prompt: "`cur.fetchall()` charge en mémoire toutes les lignes restantes du résultat, d'un seul coup.",
    courseId: "sql-sqlite3-basics",
    hints: ["Compare avec fetchone() (une seule ligne) et fetchmany(n) (n lignes)."],
    explanation:
      "Vrai : `fetchall()` récupère toutes les lignes restantes en une seule fois, en mémoire. Pour de très gros résultats, `fetchmany(n)` (par lots) ou une itération directe sur le curseur est préférable pour limiter l'empreinte mémoire.",
    correct: true,
  },
  {
    id: "sql-008",
    type: "multiple-choice",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["order-of-execution"],
    difficulty: 4,
    cognitiveLevel: "analyse",
    tags: ["order-of-execution"],
    prompt: "Dans quel ordre logique une requête SQL `SELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY` est-elle réellement exécutée ?",
    courseId: "sql-joins-groupby",
    hints: ["L'ordre d'exécution diffère de l'ordre d'écriture du texte SQL.", "SELECT n'est PAS la première étape exécutée."],
    explanation:
      "L'ordre logique réel est : `FROM`/`JOIN` (source des données) → `WHERE` (filtre les lignes brutes) → `GROUP BY` (regroupe) → `HAVING` (filtre les groupes) → `SELECT` (projette les colonnes) → `ORDER BY` (trie le résultat final). C'est pourquoi `WHERE` ne peut pas utiliser un alias défini dans `SELECT`, ni une fonction d'agrégation (réservée à `HAVING`).",
    options: [
      { id: "a", text: "FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY", correct: true },
      { id: "b", text: "SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY", correct: false, whyWrong: "SELECT n'est logiquement exécuté qu'après le filtrage et le regroupement, pas en premier." },
      { id: "c", text: "L'ordre est purement séquentiel, identique à l'ordre d'écriture du texte", correct: false, whyWrong: "L'ordre logique d'exécution diffère bien de l'ordre syntaxique d'écriture de la requête." },
      { id: "d", text: "ORDER BY s'exécute toujours avant WHERE", correct: false, whyWrong: "ORDER BY est la toute dernière étape logique, appliquée sur le résultat déjà filtré et projeté." },
    ],
  },
  {
    id: "sql-009",
    type: "code-output",
    kind: "realistic",
    level: 3,
    topic: "sql",
    subtopics: ["group-by", "realistic"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["GROUP BY", "COUNT"],
    prompt:
      "Étant donné une table `commandes(id, client_id, montant)` avec 3 commandes pour le client 1 et 2 commandes pour le client 2, que retourne cette requête ?\n\n```sql\nSELECT client_id, COUNT(*) AS nb\nFROM commandes\nGROUP BY client_id\nORDER BY nb DESC;\n```",
    code: "",
    courseId: "sql-joins-groupby",
    hints: ["GROUP BY regroupe les lignes par client_id avant de compter.", "ORDER BY nb DESC trie par nombre de commandes décroissant."],
    explanation:
      "La requête regroupe les commandes par `client_id`, compte le nombre de lignes par groupe (`COUNT(*)`), puis trie par ce compte décroissant. Le client 1 (3 commandes) apparaît avant le client 2 (2 commandes).",
    expectedOutput: "client_id=1, nb=3\nclient_id=2, nb=2",
  },
  {
    id: "sql-010",
    type: "true-false",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["injection"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["injection", "escaping"],
    prompt: "Échapper manuellement les apostrophes (`nom.replace(\"'\", \"''\")`) avant de les insérer dans une requête est une protection fiable contre l'injection SQL.",
    courseId: "sql-injections",
    hints: ["Pense aux encodages alternatifs, caractères de contrôle, ou contextes SQL non couverts par ce seul remplacement."],
    explanation:
      "Faux : l'échappement manuel manque presque toujours des cas limites (encodages spécifiques, caractères de contrôle, contextes SQL différents comme les nombres ou les identifiants). La seule protection fiable et recommandée est l'utilisation systématique de requêtes paramétrées, où le driver gère lui-même l'échappement correctement en fonction du contexte exact.",
    correct: false,
  },
  {
    id: "sql-011",
    type: "multiple-choice",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["transactions"],
    difficulty: 4,
    cognitiveLevel: "comprehension",
    tags: ["ACID", "transactions"],
    prompt: "Que signifie l'acronyme ACID appliqué aux transactions de bases de données ?",
    courseId: "sql-transactions",
    hints: ["Quatre propriétés garanties par une transaction bien implémentée."],
    explanation:
      "ACID désigne : **A**tomicité (tout ou rien), **C**ohérence (la base reste dans un état valide), **I**solation (les transactions concurrentes ne s'interfèrent pas de façon incontrôlée), **D**urabilité (une fois validée, une transaction survit même à une panne).",
    options: [
      { id: "a", text: "Atomicité, Cohérence, Isolation, Durabilité", correct: true },
      { id: "b", text: "Accès, Contrôle, Index, Données", correct: false, whyWrong: "Ce n'est pas la signification standard de cet acronyme en base de données." },
      { id: "c", text: "Async, Concurrent, Immuable, Distribué", correct: false, whyWrong: "Ces termes ne correspondent pas à la définition standard d'ACID." },
      { id: "d", text: "Un synonyme de SQL lui-même", correct: false, whyWrong: "ACID décrit des propriétés de transactions, ce n'est pas un synonyme du langage SQL." },
    ],
  },
  {
    id: "sql-012",
    type: "code-editor",
    kind: "write-code",
    level: 3,
    topic: "sql",
    subtopics: ["sqlite3", "aggregation"],
    difficulty: 4,
    cognitiveLevel: "application",
    tags: ["sqlite3", "GROUP BY", "realistic"],
    prompt:
      "Écris une fonction `total_par_client(cur)` qui reçoit un curseur sqlite3 connecté à une table `commandes(client TEXT, montant REAL)` déjà remplie, et retourne un dict `{client: total_montant}` en utilisant une requête SQL avec GROUP BY (pas de calcul Python manuel).",
    courseId: "sql-joins-groupby",
    hints: [
      "`SELECT client, SUM(montant) FROM commandes GROUP BY client`.",
      "Construis le dict à partir des tuples `(client, total)` retournés par fetchall().",
    ],
    explanation:
      "On délègue l'agrégation directement à SQL (`SUM(montant) ... GROUP BY client`), plus efficace qu'un recalcul manuel en Python, puis on construit le dict Python à partir des tuples `(client, total)` retournés.",
    starterCode: "def total_par_client(cur):\n    # Ton code ici\n    pass\n",
    solution:
      "def total_par_client(cur):\n    cur.execute(\"SELECT client, SUM(montant) FROM commandes GROUP BY client\")\n    return dict(cur.fetchall())\n",
    testCases: [
      {
        call:
          "(lambda cur: (cur.execute('CREATE TABLE commandes (client TEXT, montant REAL)'), cur.executemany('INSERT INTO commandes VALUES (?, ?)', [('A', 10.0), ('A', 5.0), ('B', 20.0)]), total_par_client(cur))[-1])(__import__('sqlite3').connect(':memory:').cursor())",
        expected: { A: 15.0, B: 20.0 },
        description: "Agrégation correcte par client",
      },
    ],
  },
  {
    id: "sql-013",
    type: "matching",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["clauses"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["SQL", "clauses"],
    prompt: "Associe chaque clause SQL à son rôle.",
    courseId: "sql-joins-groupby",
    hints: ["Pense à quelle étape du pipeline logique chaque clause intervient.", "GROUP BY regroupe, HAVING filtre les groupes."],
    explanation:
      "`WHERE` filtre les lignes individuelles avant tout regroupement. `GROUP BY` regroupe les lignes selon une ou plusieurs colonnes. `HAVING` filtre les groupes déjà formés (sur des agrégats). `ORDER BY` trie le résultat final.",
    pairs: [
      { id: "p1", left: "WHERE", right: "Filtre les lignes individuelles avant regroupement" },
      { id: "p2", left: "GROUP BY", right: "Regroupe les lignes selon une ou plusieurs colonnes" },
      { id: "p3", left: "HAVING", right: "Filtre les groupes déjà formés" },
      { id: "p4", left: "ORDER BY", right: "Trie le résultat final" },
    ],
  },
  {
    id: "sql-014",
    type: "ordering",
    kind: "code-analysis",
    level: 3,
    topic: "sql",
    subtopics: ["order-of-execution"],
    difficulty: 4,
    cognitiveLevel: "analyse",
    tags: ["order-of-execution"],
    prompt: "Classe ces étapes dans leur ORDRE D'EXÉCUTION LOGIQUE réel par le moteur SQL (pas l'ordre d'écriture du texte).",
    courseId: "sql-joins-groupby",
    hints: [
      "L'exécution ne suit pas l'ordre dans lequel on écrit la requête.",
      "FROM est toujours en premier ; SELECT arrive bien plus tard qu'on ne le pense.",
    ],
    explanation:
      "Ordre logique réel : `FROM`/`JOIN` (source des données) → `WHERE` (filtre les lignes brutes) → `GROUP BY` (regroupe) → `HAVING` (filtre les groupes) → `SELECT` (projette les colonnes) → `ORDER BY` (trie le résultat final). C'est pourquoi `WHERE` ne peut pas utiliser un alias défini dans `SELECT`, ni référencer une agrégation.",
    items: [
      { id: "from", label: "FROM" },
      { id: "where", label: "WHERE" },
      { id: "groupby", label: "GROUP BY" },
      { id: "having", label: "HAVING" },
      { id: "select", label: "SELECT" },
      { id: "orderby", label: "ORDER BY" },
    ],
    correctOrder: ["from", "where", "groupby", "having", "select", "orderby"],
  },
  {
    id: "sql-015",
    type: "multiple-choice",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["joins"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["RIGHT JOIN", "FULL OUTER JOIN"],
    prompt: "SQLite ne supporte pas nativement `RIGHT JOIN` ni `FULL OUTER JOIN` (avant la version 3.39). Comment simuler un `RIGHT JOIN A, B` avec les jointures disponibles ?",
    courseId: "sql-joins-groupby",
    source: "version-dependent",
    hints: ["Un RIGHT JOIN A, B équivaut à un LEFT JOIN avec les tables inversées.", "Il suffit d'échanger l'ordre des tables."],
    pythonVersionNote: "SQLite a ajouté le support natif de RIGHT/FULL JOIN en version 3.39 (2022) ; de nombreux environnements utilisent encore des versions antérieures.",
    explanation:
      "`A RIGHT JOIN B` est logiquement équivalent à `B LEFT JOIN A` (on garde toutes les lignes de la table qui devient la table de GAUCHE dans la version réécrite). C'est l'astuce standard pour contourner l'absence historique de RIGHT JOIN dans SQLite.",
    options: [
      { id: "a", text: "Inverser l'ordre des tables et utiliser LEFT JOIN : B LEFT JOIN A", correct: true },
      { id: "b", text: "Utiliser INNER JOIN à la place, le résultat est identique", correct: false, whyWrong: "INNER JOIN ne garde que les lignes ayant une correspondance des deux côtés, ce qui n'est pas équivalent à RIGHT JOIN." },
      { id: "c", text: "C'est impossible, il faut changer de moteur de base de données", correct: false, whyWrong: "Une simple inversion LEFT JOIN suffit, aucun changement de moteur n'est nécessaire." },
      { id: "d", text: "Utiliser CROSS JOIN puis filtrer avec WHERE", correct: false, whyWrong: "CROSS JOIN produit un produit cartésien, une base de travail inutilement coûteuse et complexe à filtrer correctement ici." },
    ],
  },
  {
    id: "sql-016",
    type: "true-false",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["sqlite3"],
    difficulty: 1,
    cognitiveLevel: "decouverte",
    tags: ["sqlite3", "serverless"],
    prompt: "SQLite est une base de données \"serverless\" : elle ne nécessite pas de processus serveur séparé, toute la base vit dans un seul fichier.",
    courseId: "sql-sqlite3-basics",
    hints: ["Compare avec PostgreSQL ou MySQL, qui nécessitent un serveur démarré séparément."],
    explanation:
      "Vrai : contrairement à PostgreSQL ou MySQL, SQLite ne fonctionne pas selon un modèle client-serveur — la bibliothèque SQLite est directement liée à l'application, et toute la base de données est stockée dans un unique fichier sur disque, sans processus serveur à administrer.",
    correct: true,
  },
  {
    id: "sql-017",
    type: "text",
    kind: "knowledge",
    level: 3,
    topic: "sql",
    subtopics: ["distinct"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["DISTINCT"],
    prompt: "Quel mot-clé SQL élimine les lignes en double d'un résultat `SELECT` ?",
    courseId: "sql-joins-groupby",
    hints: ["Il s'utilise juste après SELECT.", "C'est un adjectif anglais signifiant \"distinct/unique\"."],
    explanation:
      "`SELECT DISTINCT colonne FROM table` retourne uniquement les valeurs uniques de `colonne`, en éliminant les doublons du résultat. Appliqué à plusieurs colonnes, `DISTINCT` élimine les lignes dupliquées sur l'ensemble des colonnes sélectionnées.",
    acceptedAnswers: ["distinct", "select distinct"],
  },
  {
    id: "sql-018",
    type: "code-output",
    kind: "prediction",
    level: 3,
    topic: "sql",
    subtopics: ["subquery"],
    difficulty: 4,
    cognitiveLevel: "analyse",
    tags: ["subquery"],
    prompt: "Étant donné une table `employes(nom, salaire)` avec les salaires [3000, 4000, 5000, 6000], que retourne cette requête ?",
    code: "SELECT nom FROM employes\nWHERE salaire > (SELECT AVG(salaire) FROM employes);",
    courseId: "sql-joins-groupby",
    hints: [
      "Calcule d'abord la sous-requête : la moyenne des 4 salaires.",
      "Ensuite, quels employés ont un salaire strictement supérieur à cette moyenne ?",
    ],
    explanation:
      "La sous-requête calcule la moyenne : (3000+4000+5000+6000)/4 = 4500. La requête externe retourne les employés dont le salaire est strictement supérieur à 4500, donc ceux à 5000 et 6000.",
    expectedOutput: "Les employés avec un salaire de 5000 et de 6000",
  },
  {
    id: "sql-019",
    type: "matching",
    kind: "documentation",
    level: 3,
    topic: "sql",
    subtopics: ["aggregate-functions"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["aggregate", "SQL"],
    prompt: "Associe chaque fonction d'agrégation SQL à ce qu'elle calcule.",
    courseId: "sql-joins-groupby",
    hints: ["Chaque fonction résume une colonne entière (ou un groupe) en une seule valeur."],
    explanation:
      "`COUNT(*)` compte le nombre de lignes. `SUM(col)` additionne les valeurs. `AVG(col)` calcule la moyenne. `MAX(col)`/`MIN(col)` retournent la valeur la plus grande/petite. Toutes ces fonctions réduisent un ensemble de lignes à une seule valeur — d'où leur usage typique avec `GROUP BY`.",
    pairs: [
      { id: "p1", left: "COUNT(*)", right: "Nombre de lignes" },
      { id: "p2", left: "SUM(col)", right: "Somme des valeurs" },
      { id: "p3", left: "AVG(col)", right: "Moyenne des valeurs" },
      { id: "p4", left: "MAX(col)", right: "Valeur la plus grande" },
    ],
  },
  {
    id: "sql-020",
    type: "code-editor",
    kind: "write-code",
    level: 3,
    topic: "sql",
    subtopics: ["sqlite3", "realistic"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["sqlite3", "COUNT", "realistic"],
    prompt:
      "Écris une fonction `compter_actifs(cur)` qui reçoit un curseur sqlite3 connecté à une table `users(nom TEXT, actif INTEGER)` déjà remplie (actif vaut 0 ou 1), et retourne le nombre d'utilisateurs actifs (actif = 1), en déléguant le comptage à SQL.",
    courseId: "sql-sqlite3-basics",
    hints: [
      "`SELECT COUNT(*) FROM users WHERE actif = ?`.",
      "`cur.fetchone()` retourne un tuple à un seul élément pour ce type de requête ; il faut en extraire l'index 0.",
    ],
    explanation:
      "On délègue le comptage à SQL avec `COUNT(*)` et une requête paramétrée, plus efficace que de recharger toutes les lignes en Python pour les compter manuellement. `fetchone()[0]` extrait la valeur du tuple à un seul élément retourné.",
    starterCode: "def compter_actifs(cur):\n    # Ton code ici\n    pass\n",
    solution:
      "def compter_actifs(cur):\n    cur.execute(\"SELECT COUNT(*) FROM users WHERE actif = ?\", (1,))\n    return cur.fetchone()[0]\n",
    testCases: [
      {
        call:
          "(lambda cur: (cur.execute('CREATE TABLE users (nom TEXT, actif INTEGER)'), cur.executemany('INSERT INTO users VALUES (?, ?)', [('A', 1), ('B', 0), ('C', 1), ('D', 1)]), compter_actifs(cur))[-1])(__import__('sqlite3').connect(':memory:').cursor())",
        expected: 3,
        description: "Compte correctement les utilisateurs actifs",
      },
    ],
  },
];
