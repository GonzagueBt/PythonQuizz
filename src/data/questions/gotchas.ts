import type { Question } from "@/types";

export const gotchasQuestions: Question[] = [
  {
    id: "got-001",
    type: "code-output",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["mutable-defaults"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["mutable-default"],
    prompt: "Que va afficher ce programme ?",
    code: "def f(x, historique=[]):\n    historique.append(x)\n    return historique\n\nprint(f(1))\nprint(f(2))",
    courseId: "gotchas-mutable-defaults",
    hints: ["La liste par défaut n'est créée qu'une seule fois, à la définition de la fonction."],
    explanation:
      "La liste par défaut `[]` est créée une seule fois et partagée entre tous les appels qui ne fournissent pas `historique` explicitement. Le premier appel produit `[1]`, mais le second réutilise la même liste, déjà modifiée : `[1, 2]`.",
    expectedOutput: "[1]\n[1, 2]",
  },
  {
    id: "got-002",
    type: "true-false",
    kind: "gotcha",
    level: 4,
    topic: "gotchas",
    subtopics: ["identity"],
    difficulty: 4,
    cognitiveLevel: "piege",
    tags: ["is", "interning"],
    prompt: "`a = 5; b = 5; a is b` retourne `True` de façon fiable et garantie par le langage Python.",
    courseId: "gotchas-identity-caching",
    source: "cpython",
    hints: ["Le comportement observé (True) est réel sur CPython, mais est-il GARANTI par la spécification du langage ?"],
    explanation:
      "Le résultat observé (`True`) est correct en pratique sur CPython actuel (5 est dans la plage internée -5 à 256), mais ce n'est **pas garanti par le langage Python** — c'est un détail d'optimisation propre à l'implémentation CPython, qui pourrait théoriquement changer. Ne jamais s'appuyer sur `is` pour comparer des valeurs numériques dans du code destiné à être portable et robuste.",
    correct: false,
  },
  {
    id: "got-003",
    type: "code-output",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["list-multiplication"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["list-multiplication"],
    prompt: "Que va afficher ce programme ?",
    code: "x = [[]] * 3\nx[0].append(1)\nprint(x)",
    courseId: "gotchas-list-multiplication",
    hints: ["Les trois éléments de x référencent-ils trois listes distinctes, ou la même ?"],
    explanation:
      "`[[]] * 3` duplique trois fois la **référence** vers l'unique liste interne créée — les trois \"sous-listes\" sont en réalité un seul et même objet. Modifier `x[0]` modifie donc apparemment les trois. Résultat : `[[1], [1], [1]]`.",
    expectedOutput: "[[1], [1], [1]]",
  },
  {
    id: "got-004",
    type: "multiple-choice",
    kind: "gotcha",
    level: 4,
    topic: "gotchas",
    subtopics: ["floats"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["floats", "IEEE754"],
    prompt: "Pourquoi `0.1 + 0.2 == 0.3` retourne-t-il `False` en Python ?",
    courseId: "gotchas-floats",
    hints: ["Ce n'est pas spécifique à Python.", "Pense à la représentation binaire des nombres à virgule flottante."],
    explanation:
      "Les flottants sont stockés en binaire à virgule flottante double précision (IEEE 754), qui ne peut représenter exactement que certaines fractions (puissances de 2). `0.1` et `0.2` n'ont pas de représentation binaire exacte, donc leur somme accumule une infime erreur d'arrondi (`0.30000000000000004`), différente de la représentation de `0.3`. Ce comportement est standard dans tout langage utilisant IEEE 754, pas un bug de Python.",
    options: [
      { id: "a", text: "0.1 et 0.2 n'ont pas de représentation binaire exacte (norme IEEE 754)", correct: true },
      { id: "b", text: "C'est un bug connu de l'interpréteur Python", correct: false, whyWrong: "C'est un comportement standard de la norme IEEE 754, partagé par la quasi-totalité des langages de programmation modernes." },
      { id: "c", text: "== ne fonctionne pas avec les nombres décimaux en Python", correct: false, whyWrong: "== fonctionne normalement avec les flottants ; le souci est la précision de représentation, pas l'opérateur lui-même." },
      { id: "d", text: "Il faut toujours écrire 0.10 et 0.20 avec deux décimales", correct: false, whyWrong: "Le nombre de zéros écrits dans le littéral n'a aucune influence sur sa représentation binaire interne." },
    ],
  },
  {
    id: "got-005",
    type: "code-output",
    kind: "gotcha",
    level: 3,
    topic: "gotchas",
    subtopics: ["late-binding"],
    difficulty: 4,
    cognitiveLevel: "piege",
    tags: ["closures", "late-binding"],
    prompt: "Que va afficher ce programme ?",
    code: "fonctions = [lambda: i for i in range(3)]\nprint([f() for f in fonctions])",
    courseId: "gotchas-late-binding-closures",
    hints: ["Toutes les lambdas partagent la même variable `i` par closure (liaison tardive)."],
    explanation:
      "Chaque lambda capture la variable `i` (par référence), pas sa valeur au moment de sa création. Une fois la boucle terminée, `i` vaut `2`, donc les trois lambdas retournent toutes `2`. Résultat : `[2, 2, 2]`.",
    expectedOutput: "[2, 2, 2]",
  },
  {
    id: "got-006",
    type: "text",
    kind: "fix-code",
    level: 3,
    topic: "gotchas",
    subtopics: ["late-binding"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["closures", "fix"],
    prompt: "Corrige `[lambda: i for i in range(3)]` pour que chaque lambda retourne bien sa valeur d'origine (0, 1, 2). Donne juste la nouvelle expression lambda utilisée dans la compréhension.",
    courseId: "gotchas-late-binding-closures",
    hints: ["Force l'évaluation immédiate de `i` via un argument par défaut, qui lui EST évalué à la création."],
    explanation:
      "`lambda i=i: i` force l'évaluation de `i` immédiatement à la création de chaque lambda (les valeurs par défaut sont évaluées une seule fois, à la définition), au lieu de la capturer par référence tardive. Chaque lambda a alors son propre argument `i` figé.",
    acceptedAnswers: ["lambda i=i: i", "lambda i=i:i"],
  },
  {
    id: "got-007",
    type: "code-output",
    kind: "gotcha",
    level: 1,
    topic: "gotchas",
    subtopics: ["aliasing"],
    difficulty: 2,
    cognitiveLevel: "piege",
    tags: ["aliasing", "references"],
    prompt: "Que va afficher ce programme ?",
    code: "a = [1, 2, 3]\nb = a\nb.append(4)\nprint(a is b, a)",
    courseId: "gotchas-aliasing",
    hints: ["`b = a` ne copie pas la liste : les deux noms référencent le même objet."],
    explanation:
      "`b = a` fait de `b` une seconde étiquette pour le **même objet** que `a` (`a is b` vaut `True`). Modifier `b` via `append` modifie donc l'objet partagé, visible aussi à travers `a`. Résultat : `True [1, 2, 3, 4]`.",
    expectedOutput: "True [1, 2, 3, 4]",
  },
  {
    id: "got-008",
    type: "multiple-choice",
    kind: "gotcha",
    level: 1,
    topic: "gotchas",
    subtopics: ["aliasing"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["aliasing", "fix"],
    prompt: "Quelle expression crée une VRAIE copie indépendante d'une liste `a`, contrairement à `b = a` ?",
    courseId: "gotchas-aliasing",
    hints: ["Il existe plusieurs syntaxes équivalentes pour une copie superficielle."],
    explanation:
      "`a.copy()`, `a[:]` (slice complet) et `list(a)` créent tous une nouvelle liste indépendante, contenant les mêmes éléments (par référence, copie superficielle) — mais pas la même liste que `a`. Modifier cette copie n'affecte pas `a`.",
    options: [
      { id: "a", text: "b = a.copy()", correct: true },
      { id: "b", text: "b = a", correct: false, whyWrong: "C'est justement l'affectation qui NE copie PAS, créant l'aliasing du piège." },
      { id: "c", text: "b == a", correct: false, whyWrong: "== est une comparaison, elle ne crée ni ne modifie aucun objet, et ne produit pas de nouvelle liste." },
      { id: "d", text: "b = a; a = []", correct: false, whyWrong: "Cela laisserait b référencer l'ancien objet (donc pas de copie), et viderait le nom a, ce qui n'a rien à voir avec une copie de contenu." },
    ],
  },
  {
    id: "got-009",
    type: "true-false",
    kind: "gotcha",
    level: 1,
    topic: "gotchas",
    subtopics: ["boolean"],
    difficulty: 2,
    cognitiveLevel: "piege",
    tags: ["bool", "equality"],
    prompt: "`True == 1` retourne `True` en Python.",
    courseId: "gotchas-identity-caching",
    hints: ["bool hérite de int dans le modèle objet de Python."],
    explanation:
      "Vrai : `bool` est une sous-classe d'`int` en Python, et `True`/`False` équivalent numériquement à `1`/`0`. `True == 1` retourne donc `True` (bien que `True is 1` retourne `False`, car ce sont deux objets distincts).",
    correct: true,
  },
  {
    id: "got-010",
    type: "code-output",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["mutable-defaults", "dict"],
    difficulty: 4,
    cognitiveLevel: "piege",
    tags: ["mutable-default", "dict"],
    prompt: "Que va afficher ce programme ?",
    code:
      "def ajouter_score(nom, points, scores={}):\n    scores[nom] = scores.get(nom, 0) + points\n    return scores\n\nprint(ajouter_score(\"A\", 10))\nprint(ajouter_score(\"B\", 5))",
    courseId: "gotchas-mutable-defaults",
    hints: ["Le dict par défaut, comme la liste par défaut, est créé une seule fois et partagé."],
    explanation:
      "Le dictionnaire par défaut `{}` est créé une seule fois et partagé entre appels successifs sans argument `scores` explicite. Le premier appel produit `{'A': 10}`. Le second appel réutilise ce même dict partagé et y ajoute `'B'`, donnant `{'A': 10, 'B': 5}` — pas un dict frais contenant uniquement `'B'`.",
    expectedOutput: "{'A': 10}\n{'A': 10, 'B': 5}",
  },
  {
    id: "got-011",
    type: "multiple-choice",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["chained-comparison"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["chained-comparison"],
    prompt: "Que retourne `1 < 2 < 0` ?",
    courseId: "fundamentals-operators",
    hints: ["Les comparaisons chaînées sont équivalentes à un `and` entre chaque paire consécutive."],
    explanation:
      "Les comparaisons chaînées en Python équivalent à `1 < 2 and 2 < 0`. `1 < 2` est vrai, mais `2 < 0` est faux, donc le `and` retourne `False`. Ce comportement diffère de langages comme JavaScript, où `1 < 2 < 0` serait évalué de gauche à droite comme `(1 < 2) < 0`, soit `True < 0`, soit `1 < 0`, soit `False` — ici par coïncidence le même résultat final, mais via une logique totalement différente.",
    options: [
      { id: "a", text: "False", correct: true },
      { id: "b", text: "True", correct: false, whyWrong: "2 < 0 est faux, ce qui rend toute la chaîne de comparaison fausse." },
      { id: "c", text: "Une erreur est levée", correct: false, whyWrong: "Les comparaisons chaînées sont une syntaxe parfaitement valide en Python, sans lever d'erreur." },
      { id: "d", text: "0", correct: false, whyWrong: "Une comparaison chaînée retourne toujours un booléen, jamais une des valeurs comparées." },
    ],
  },
  {
    id: "got-012",
    type: "code-output",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["string-multiplication"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["strings"],
    prompt: "Que va afficher ce programme ?",
    code: "print(\"ab\" * 3)",
    courseId: "fundamentals-types",
    hints: ["La multiplication d'une chaîne par un entier répète la chaîne."],
    explanation:
      "`\"ab\" * 3` répète la chaîne 3 fois, sans séparateur : `\"ababab\"`. C'est une opération native, non liée au piège de duplication de référence (les chaînes étant immuables, cette répétition est sûre et prévisible).",
    expectedOutput: "ababab",
  },
  {
    id: "got-013",
    type: "true-false",
    kind: "gotcha",
    level: 4,
    topic: "gotchas",
    subtopics: ["is-none"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["None", "is"],
    prompt: "`x is None` est plus fiable que `x == None` pour tester si `x` vaut `None`.",
    courseId: "gotchas-identity-caching",
    hints: ["`None` est un singleton : une seule instance existe dans tout le programme.", "Un `__eq__` personnalisé pourrait redéfinir == de façon surprenante."],
    explanation:
      "Vrai. `None` est un singleton unique, donc `is` teste directement et fiablement l'identité. `==` pourrait en théorie être redéfini par un `__eq__` personnalisé sur une classe, produisant un résultat surprenant face à `None` — `is` évite ce risque, en plus d'être légèrement plus rapide.",
    correct: true,
  },
  {
    id: "got-014",
    type: "code-output",
    kind: "gotcha",
    level: 2,
    topic: "gotchas",
    subtopics: ["exceptions", "finally"],
    difficulty: 4,
    cognitiveLevel: "piege",
    tags: ["finally", "return"],
    prompt: "Que retourne l'appel `f()` ci-dessous ?",
    code: "def f():\n    try:\n        return 1\n    finally:\n        return 2\n\nprint(f())",
    courseId: "fundamentals-exceptions",
    hints: ["`finally` s'exécute TOUJOURS, même après un `return` dans le `try`.", "Un `return` dans `finally` écrase le `return` précédent."],
    explanation:
      "Le `return 1` du `try` est \"programmé\", mais avant que la fonction ne se termine réellement, le bloc `finally` s'exécute — et contient lui-même un `return 2`, qui **écrase** la valeur de retour précédente. C'est un piège classique et une raison pour laquelle il est déconseillé d'utiliser `return` dans un `finally`. Résultat : `2`.",
    expectedOutput: "2",
  },
  {
    id: "got-015",
    type: "multiple-choice",
    kind: "gotcha",
    level: 3,
    topic: "gotchas",
    subtopics: ["default-args-evaluation"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["default-args"],
    prompt: "Quand la valeur par défaut `datetime.now()` dans `def log(msg, ts=datetime.now()): ...` est-elle évaluée ?",
    courseId: "gotchas-mutable-defaults",
    hints: ["Les valeurs par défaut suivent la même règle que les listes/dicts par défaut : une seule évaluation.", "Ce n'est PAS à chaque appel."],
    explanation:
      "Comme toute valeur par défaut, `datetime.now()` n'est évaluée qu'**une seule fois**, au moment de la définition de la fonction — pas à chaque appel. Tous les appels sans `ts` explicite reçoivent donc le même timestamp figé (celui du moment où le module a été chargé), pas l'heure actuelle de l'appel. C'est un piège fréquent, structurellement identique à celui des listes par défaut mutables.",
    options: [
      { id: "a", text: "Une seule fois, à la définition de la fonction", correct: true },
      { id: "b", text: "À chaque appel de la fonction", correct: false, whyWrong: "C'est le comportement intuitivement attendu, mais ce n'est pas celui de Python pour les valeurs par défaut." },
      { id: "c", text: "Seulement si `ts` n'est jamais fourni explicitement", correct: false, whyWrong: "L'évaluation a lieu à la définition, indépendamment du fait que ts soit fourni ou non lors des appels suivants." },
      { id: "d", text: "Jamais, `datetime.now()` reste une référence non évaluée", correct: false, whyWrong: "L'expression est bien évaluée pour produire une valeur concrète, une seule fois, comme toute valeur par défaut." },
    ],
  },
];
