import type { Question } from "@/types";

export const stdlibQuestions: Question[] = [
  {
    id: "std-001",
    type: "text",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["pathlib"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["pathlib"],
    prompt: "Quel opérateur `pathlib.Path` surcharge pour permettre de joindre des composants de chemin (ex. `Path(\"a\") / \"b\"`) ?",
    courseId: "stdlib-os-pathlib",
    hints: ["C'est un opérateur arithmétique habituellement utilisé pour la division."],
    explanation:
      "`pathlib.Path` surcharge l'opérateur `/` pour joindre des segments de chemin de façon lisible et portable entre systèmes d'exploitation : `Path(\"data\") / \"fichiers\" / \"a.csv\"`, plutôt que la concaténation manuelle de chaînes propre à `os.path.join`.",
    acceptedAnswers: ["/", "division", "slash"],
  },
  {
    id: "std-002",
    type: "code-output",
    kind: "prediction",
    level: 2,
    topic: "stdlib",
    subtopics: ["collections"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["Counter"],
    prompt: "Que va afficher ce programme ?",
    code: "from collections import Counter\nc = Counter(\"abracadabra\")\nprint(c.most_common(2))",
    courseId: "stdlib-collections",
    hints: ["Counter compte les occurrences de chaque caractère.", "`most_common(n)` retourne les n plus fréquents, triés par fréquence décroissante."],
    explanation:
      "`Counter(\"abracadabra\")` compte : a=5, b=2, r=2, c=1, d=1. `most_common(2)` retourne les deux plus fréquents sous forme de liste de tuples `(élément, compte)`, triés par fréquence décroissante : `[('a', 5), ('b', 2)]` (b et r sont à égalité de fréquence, mais `Counter` conserve l'ordre de première apparition en cas d'égalité).",
    expectedOutput: "[('a', 5), ('b', 2)]",
  },
  {
    id: "std-003",
    type: "multiple-choice",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["collections"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["defaultdict"],
    prompt: "Que fait `defaultdict(list)` quand on accède à une clé absente ?",
    courseId: "stdlib-collections",
    hints: ["Le comportement diffère d'un dict classique, qui lèverait une KeyError.", "Une fabrique (factory) est appelée automatiquement."],
    explanation:
      "`defaultdict(list)` appelle automatiquement `list()` (sans argument) pour créer une valeur par défaut dès qu'une clé absente est accédée, l'insère dans le dictionnaire, puis la retourne — évitant d'avoir à vérifier manuellement `if clé not in dict` avant d'initialiser une liste vide.",
    options: [
      { id: "a", text: "Crée automatiquement une liste vide pour cette clé et la retourne", correct: true },
      { id: "b", text: "Lève une KeyError, comme un dict normal", correct: false, whyWrong: "C'est justement le comportement que defaultdict est conçu pour éviter." },
      { id: "c", text: "Retourne None sans créer d'entrée", correct: false, whyWrong: "Une entrée EST créée dans le dictionnaire, avec une liste vide comme valeur, pas juste un retour de None." },
      { id: "d", text: "Lève une TypeError car list n'est pas une valeur valide", correct: false, whyWrong: "list (la fonction/classe elle-même) est précisément l'argument attendu par defaultdict comme fabrique de valeur par défaut." },
    ],
  },
  {
    id: "std-004",
    type: "true-false",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["collections", "deque"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["deque"],
    prompt: "`collections.deque` offre une complexité O(1) pour ajouter/retirer des éléments aux DEUX extrémités.",
    courseId: "stdlib-collections",
    hints: ["Contrairement à une liste Python classique, optimisée uniquement pour la fin."],
    explanation:
      "Vrai : `deque` est implémenté comme une liste doublement chaînée par blocs, offrant O(1) pour `append`/`appendleft`/`pop`/`popleft` — contrairement à `list`, où seule la fin est O(1) amorti (insérer/retirer en tête est O(n)).",
    correct: true,
  },
  {
    id: "std-005",
    type: "multiple-choice",
    kind: "code-analysis",
    level: 2,
    topic: "stdlib",
    subtopics: ["itertools"],
    difficulty: 3,
    cognitiveLevel: "analyse",
    tags: ["groupby"],
    prompt: "Pourquoi `itertools.groupby` peut-il produire des groupes \"dupliqués\" si les données ne sont pas triées au préalable ?",
    courseId: "stdlib-itertools-functools",
    hints: ["`groupby` ne regarde que les éléments CONSÉCUTIFS.", "Deux éléments avec la même clé mais séparés par d'autres éléments forment deux groupes distincts."],
    explanation:
      "`groupby` regroupe uniquement les éléments **consécutifs** partageant la même clé — il ne réorganise jamais les données. Si les données ne sont pas triées par cette clé au préalable, deux éléments avec la même clé mais séparés dans la séquence par d'autres clés formeront deux groupes distincts au lieu d'un seul. Il faut donc systématiquement trier avant d'appeler `groupby`.",
    options: [
      { id: "a", text: "groupby ne regroupe que les éléments consécutifs, sans trier les données lui-même", correct: true },
      { id: "b", text: "groupby a une limite de 2 groupes maximum", correct: false, whyWrong: "Il n'existe aucune limite de ce type dans groupby." },
      { id: "c", text: "C'est un bug connu de la bibliothèque standard", correct: false, whyWrong: "C'est un comportement documenté et attendu, pas un bug." },
      { id: "d", text: "groupby ne fonctionne qu'avec des nombres", correct: false, whyWrong: "groupby fonctionne avec n'importe quel type de clé, du moment qu'elle est comparable par égalité." },
    ],
  },
  {
    id: "std-006",
    type: "code-output",
    kind: "prediction",
    level: 2,
    topic: "stdlib",
    subtopics: ["functools"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["reduce"],
    prompt: "Que va afficher ce programme ?",
    code: "from functools import reduce\nresultat = reduce(lambda a, b: a * b, [1, 2, 3, 4])\nprint(resultat)",
    courseId: "stdlib-itertools-functools",
    hints: ["`reduce` applique la fonction cumulativement, de gauche à droite.", "1*2=2, puis 2*3=6, puis 6*4=..."],
    explanation:
      "`reduce(f, [1,2,3,4])` calcule `f(f(f(1,2),3),4)`. Avec la multiplication : `1*2=2`, `2*3=6`, `6*4=24`. C'est en fait le calcul de `4!` (factorielle de 4).",
    expectedOutput: "24",
  },
  {
    id: "std-007",
    type: "multiple-choice",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["re"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["re", "match-vs-search"],
    prompt: "Quelle est la différence entre `re.match(pattern, s)` et `re.search(pattern, s)` ?",
    courseId: "stdlib-re-datetime",
    hints: ["Pense à où chacune commence à chercher dans la chaîne."],
    explanation:
      "`re.match` ancre systématiquement au **début** de la chaîne (le pattern doit correspondre dès le premier caractère). `re.search` cherche une correspondance n'importe où dans la chaîne, en s'arrêtant à la première trouvée. Il existe aussi `re.fullmatch`, qui exige que le pattern couvre **toute** la chaîne.",
    options: [
      { id: "a", text: "match ancre au début de la chaîne, search cherche n'importe où", correct: true },
      { id: "b", text: "search ancre au début, match cherche n'importe où", correct: false, whyWrong: "C'est l'inverse de leur comportement réel." },
      { id: "c", text: "Ce sont deux alias exactement équivalents", correct: false, whyWrong: "Leur comportement diffère bien concrètement sur des chaînes où le motif n'apparaît pas dès le début." },
      { id: "d", text: "match est pour les nombres, search pour le texte", correct: false, whyWrong: "Les deux fonctionnent identiquement sur n'importe quel type de motif texte." },
    ],
  },
  {
    id: "std-008",
    type: "true-false",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["datetime"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["datetime", "timezone"],
    prompt: "On peut comparer directement un `datetime` naïf (sans fuseau horaire) avec un `datetime` aware (avec fuseau horaire).",
    courseId: "stdlib-re-datetime",
    hints: ["Python considère ces deux catégories comme incompatibles pour la comparaison directe."],
    explanation:
      "Faux : comparer un `datetime` naïf et un `datetime` aware lève `TypeError: can't compare offset-naive and offset-aware datetimes`. Il faut soit rendre les deux aware (en leur assignant un fuseau via `tzinfo`), soit les deux naïfs, avant de les comparer.",
    correct: false,
  },
  {
    id: "std-009",
    type: "code-editor",
    kind: "write-code",
    level: 2,
    topic: "stdlib",
    subtopics: ["json"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["json"],
    prompt:
      "Écris une fonction `to_json_keys(d)` qui reçoit un dict et retourne la liste TRIÉE de ses clés après un aller-retour JSON (json.dumps puis json.loads).",
    courseId: "stdlib-json-csv",
    hints: [
      "`json.dumps(d)` sérialise en chaîne, `json.loads(...)` désérialise.",
      "`sorted(dico.keys())` retourne la liste triée des clés.",
    ],
    explanation:
      "On sérialise puis désérialise le dictionnaire (`json.loads(json.dumps(d))`), ce qui simule un aller-retour réseau/fichier typique, puis on retourne `sorted(resultat.keys())`.",
    starterCode: "import json\n\ndef to_json_keys(d):\n    # Ton code ici\n    pass\n",
    solution: "import json\n\ndef to_json_keys(d):\n    resultat = json.loads(json.dumps(d))\n    return sorted(resultat.keys())\n",
    testCases: [
      { call: 'to_json_keys({"b": 1, "a": 2})', expected: ["a", "b"], description: "Clés triées alphabétiquement" },
      { call: "to_json_keys({})", expected: [], description: "Dictionnaire vide" },
    ],
  },
  {
    id: "std-010",
    type: "multiple-choice",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["csv"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["csv", "DictReader"],
    prompt: "Que retourne chaque ligne itérée d'un `csv.DictReader` ?",
    courseId: "stdlib-json-csv",
    hints: ["Le nom du type contient un indice évident.", "La première ligne du fichier sert de source pour un élément particulier."],
    explanation:
      "`csv.DictReader` utilise la première ligne du fichier comme en-têtes de colonnes, puis retourne chaque ligne suivante sous forme de `dict` (ou `OrderedDict` selon la version), mappant chaque en-tête à la valeur correspondante de la ligne — plus pratique que `csv.reader`, qui retourne de simples listes indexées par position.",
    options: [
      { id: "a", text: "Un dictionnaire {en-tête: valeur}", correct: true },
      { id: "b", text: "Une liste de valeurs, comme csv.reader", correct: false, whyWrong: "C'est le comportement de csv.reader, pas de DictReader." },
      { id: "c", text: "Une chaîne brute contenant toute la ligne", correct: false, whyWrong: "Le module csv parse déjà la ligne en valeurs séparées, jamais en chaîne brute unique." },
      { id: "d", text: "Un tuple nommé (namedtuple)", correct: false, whyWrong: "DictReader retourne des dict, pas des namedtuples (ce comportement existe dans pandas.itertuples, pas ici)." },
    ],
  },
  {
    id: "std-011",
    type: "fill-code",
    kind: "write-code",
    level: 2,
    topic: "stdlib",
    subtopics: ["sqlite3", "context-manager"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["subprocess"],
    prompt: "Complète cet appel `subprocess.run` pour capturer la sortie sous forme de texte (pas de bytes) et lever une exception en cas d'échec.",
    courseId: "stdlib-subprocess-logging",
    hints: [
      "Il faut deux paramètres booléens précis.",
      "L'un capture stdout/stderr, l'autre décode automatiquement en texte, le dernier fait lever une exception si le code de sortie est non nul.",
    ],
    explanation:
      "`capture_output=True` capture stdout/stderr, `text=True` les décode automatiquement en chaînes (au lieu de bytes bruts), et `check=True` lève `CalledProcessError` si la commande retourne un code de sortie non nul.",
    codeTemplate:
      'subprocess.run(["ls", "-la"], capture_output=True, {{blank:text}}, {{blank:check}})',
    blanks: [
      { id: "text", acceptedAnswers: ["text=True"] },
      { id: "check", acceptedAnswers: ["check=True"] },
    ],
  },
  {
    id: "std-012",
    type: "true-false",
    kind: "knowledge",
    level: 3,
    topic: "stdlib",
    subtopics: ["logging"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["logging", "print"],
    prompt: "`logging` est généralement préférable à `print()` dans du code applicatif de production.",
    courseId: "stdlib-subprocess-logging",
    hints: ["Pense aux niveaux de sévérité, à la configuration centralisée, aux destinations multiples (fichier, réseau)."],
    explanation:
      "Vrai : `logging` offre des niveaux de sévérité (DEBUG à CRITICAL), une configuration centralisée (activable/désactivable sans toucher au code appelant), et la possibilité de rediriger la sortie vers des fichiers, le réseau, ou plusieurs destinations à la fois — bien plus flexible qu'un simple `print()` disséminé dans le code.",
    correct: true,
  },
  {
    id: "std-013",
    type: "multiple-choice",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["itertools"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["itertools", "chain"],
    prompt: "Que retourne `list(itertools.chain([1, 2], [3, 4]))` ?",
    courseId: "stdlib-itertools-functools",
    hints: ["`chain` concatène plusieurs itérables en un seul flux, sans les copier au préalable."],
    explanation:
      "`itertools.chain` enchaîne plusieurs itérables en un seul flux paresseux, sans jamais construire de structure intermédiaire — équivalent fonctionnellement à une concaténation, mais plus efficace en mémoire pour de grands itérables. Résultat : `[1, 2, 3, 4]`.",
    options: [
      { id: "a", text: "[1, 2, 3, 4]", correct: true },
      { id: "b", text: "[[1, 2], [3, 4]]", correct: false, whyWrong: "chain aplatit les itérables au premier niveau, il ne les imbrique pas." },
      { id: "c", text: "[1, 3, 2, 4]", correct: false, whyWrong: "chain ne mélange pas les éléments un par un ; il concatène séquentiellement chaque itérable entier." },
      { id: "d", text: "Une erreur, chain nécessite des itérables de même taille", correct: false, whyWrong: "chain n'a aucune contrainte de taille entre les itérables, contrairement à zip." },
    ],
  },
  {
    id: "std-014",
    type: "code-output",
    kind: "realistic",
    level: 2,
    topic: "stdlib",
    subtopics: ["operator"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["operator", "itemgetter"],
    prompt: "Que va afficher ce programme ?",
    code:
      "from operator import itemgetter\ndonnees = [{\"nom\": \"B\", \"score\": 10}, {\"nom\": \"A\", \"score\": 20}]\ntries = sorted(donnees, key=itemgetter(\"score\"))\nprint([d[\"nom\"] for d in tries])",
    courseId: "stdlib-itertools-functools",
    hints: ["`itemgetter(\"score\")` produit une fonction équivalente à `lambda d: d[\"score\"]`.", "Le tri se fait par score CROISSANT (pas de reverse=True)."],
    explanation:
      "`itemgetter(\"score\")` crée une fonction extrayant la clé `\"score\"` de chaque élément, utilisée ici comme critère de tri croissant : `B` (score 10) avant `A` (score 20). Résultat : `['B', 'A']`.",
    expectedOutput: "['B', 'A']",
  },
  {
    id: "std-015",
    type: "matching",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["modules"],
    difficulty: 2,
    cognitiveLevel: "comprehension",
    tags: ["stdlib", "modules"],
    prompt: "Associe chaque module de la bibliothèque standard à son domaine principal.",
    courseId: "stdlib-itertools-functools",
    hints: ["Pense au nom du module : il est souvent très évocateur de son contenu."],
    explanation:
      "`itertools` fournit des outils d'itération combinatoire efficaces en mémoire. `functools` fournit des outils de programmation fonctionnelle (mémoïsation, composition). `collections` fournit des structures de données spécialisées (Counter, defaultdict, deque). `pathlib` fournit une API orientée objet pour manipuler des chemins de fichiers.",
    pairs: [
      { id: "p1", left: "itertools", right: "Outils d'itération combinatoire" },
      { id: "p2", left: "functools", right: "Programmation fonctionnelle (cache, composition)" },
      { id: "p3", left: "collections", right: "Structures de données spécialisées" },
      { id: "p4", left: "pathlib", right: "Manipulation orientée objet des chemins" },
    ],
  },
  {
    id: "std-016",
    type: "ordering",
    kind: "code-analysis",
    level: 1,
    topic: "stdlib",
    subtopics: ["csv"],
    difficulty: 2,
    cognitiveLevel: "application",
    tags: ["csv", "context-manager"],
    prompt: "Classe ces étapes dans l'ordre pour lire correctement un fichier CSV avec le module csv.",
    courseId: "stdlib-json-csv",
    hints: ["On ne peut pas créer un DictReader avant d'avoir ouvert le fichier.", "Le fichier se ferme automatiquement à la sortie du bloc with."],
    explanation:
      "On ouvre d'abord le fichier avec `open()` dans un bloc `with` (garantissant sa fermeture automatique). On crée ensuite un `csv.DictReader` sur ce fichier ouvert. On itère alors sur ce reader pour traiter chaque ligne. Le fichier se ferme automatiquement à la sortie du bloc `with`, sans étape explicite supplémentaire.",
    items: [
      { id: "open", label: "Ouvrir le fichier avec open() dans un with" },
      { id: "reader", label: "Créer un csv.DictReader sur le fichier" },
      { id: "iterate", label: "Itérer sur le reader pour traiter chaque ligne" },
    ],
    correctOrder: ["open", "reader", "iterate"],
  },
  {
    id: "std-017",
    type: "multiple-choice",
    kind: "knowledge",
    level: 2,
    topic: "stdlib",
    subtopics: ["itertools"],
    difficulty: 3,
    cognitiveLevel: "comprehension",
    tags: ["itertools", "combinatorics"],
    prompt: "Quelle est la différence entre `itertools.permutations([1,2,3], 2)` et `itertools.combinations([1,2,3], 2)` ?",
    courseId: "stdlib-itertools-functools",
    hints: ["L'un des deux considère (1,2) et (2,1) comme deux résultats distincts.", "L'autre ne les compte qu'une seule fois."],
    explanation:
      "`permutations` tient compte de l'**ordre** : `(1,2)` et `(2,1)` sont deux résultats distincts, produisant 6 paires au total pour n=3, k=2. `combinations` ignore l'ordre : `(1,2)` et `(2,1)` sont considérés comme identiques et ne comptent qu'une fois, produisant seulement 3 paires.",
    options: [
      { id: "a", text: "permutations tient compte de l'ordre, combinations l'ignore", correct: true },
      { id: "b", text: "combinations tient compte de l'ordre, permutations l'ignore", correct: false, whyWrong: "C'est l'inverse de leur comportement réel." },
      { id: "c", text: "Ce sont deux alias produisant exactement le même résultat", correct: false, whyWrong: "Leurs résultats diffèrent bien en nombre et en contenu (6 vs 3 paires ici)." },
      { id: "d", text: "permutations ne fonctionne qu'avec des chaînes de caractères", correct: false, whyWrong: "permutations fonctionne avec n'importe quel itérable, pas seulement des chaînes." },
    ],
  },
  {
    id: "std-018",
    type: "true-false",
    kind: "gotcha",
    level: 2,
    topic: "stdlib",
    subtopics: ["pathlib"],
    difficulty: 3,
    cognitiveLevel: "piege",
    tags: ["pathlib", "equality"],
    prompt: "`Path(\"a/b\") == \"a/b\"` (comparer un objet Path à une chaîne) retourne `True`.",
    courseId: "stdlib-os-pathlib",
    hints: ["Pense aux types comparés : un objet Path et une chaîne de caractères sont-ils du même type ?"],
    explanation:
      "Faux : un objet `Path` n'est jamais égal à une chaîne de caractères brute via `==`, même si leur représentation textuelle est identique — ce sont des types différents. Pour comparer, il faut soit convertir explicitement (`str(chemin) == \"a/b\"`), soit comparer deux objets `Path` entre eux (`Path(\"a/b\") == Path(\"a/b\")`, qui lui retourne bien `True`).",
    correct: false,
  },
  {
    id: "std-019",
    type: "code-output",
    kind: "prediction",
    level: 2,
    topic: "stdlib",
    subtopics: ["collections"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["defaultdict", "realistic"],
    prompt: "Que va afficher ce programme ?",
    code:
      "from collections import defaultdict\n\nmots = [\"chat\", \"chien\", \"cheval\", \"vache\"]\ngroupes = defaultdict(list)\nfor mot in mots:\n    groupes[mot[0]].append(mot)\n\nprint(dict(groupes))",
    courseId: "stdlib-collections",
    hints: [
      "Chaque mot est ajouté à la liste correspondant à sa première lettre.",
      "`defaultdict(list)` crée automatiquement une liste vide pour chaque nouvelle clé rencontrée.",
    ],
    explanation:
      "Le programme regroupe les mots par première lettre : \"chat\", \"chien\", \"cheval\" commencent par 'c', \"vache\" par 'v'. `defaultdict(list)` crée automatiquement la liste vide pour chaque nouvelle lettre rencontrée. Résultat : `{'c': ['chat', 'chien', 'cheval'], 'v': ['vache']}`.",
    expectedOutput: "{'c': ['chat', 'chien', 'cheval'], 'v': ['vache']}",
  },
  {
    id: "std-020",
    type: "code-editor",
    kind: "write-code",
    level: 2,
    topic: "stdlib",
    subtopics: ["collections", "realistic"],
    difficulty: 3,
    cognitiveLevel: "application",
    tags: ["Counter", "realistic"],
    prompt:
      "Écris une fonction `mot_le_plus_frequent(texte)` qui reçoit une chaîne de mots séparés par des espaces et retourne le mot le plus fréquent, en utilisant `collections.Counter`.",
    courseId: "stdlib-collections",
    hints: [
      "`texte.split()` découpe la chaîne en liste de mots.",
      "`Counter(liste_mots).most_common(1)` retourne le mot le plus fréquent sous forme de liste d'un seul tuple `(mot, compte)`.",
    ],
    explanation:
      "On découpe le texte en mots avec `.split()`, on construit un `Counter` pour compter les occurrences, puis `.most_common(1)` retourne le mot le plus fréquent sous forme `[(mot, compte)]` — on extrait `[0][0]` pour obtenir juste le mot.",
    starterCode: "from collections import Counter\n\ndef mot_le_plus_frequent(texte):\n    # Ton code ici\n    pass\n",
    solution:
      "from collections import Counter\n\ndef mot_le_plus_frequent(texte):\n    compteur = Counter(texte.split())\n    return compteur.most_common(1)[0][0]\n",
    testCases: [
      { call: "mot_le_plus_frequent('le chat le chien le chat')", expected: "le", description: "Le mot 'le' apparaît 3 fois" },
      { call: "mot_le_plus_frequent('a b a')", expected: "a", description: "Cas simple à deux mots distincts" },
    ],
  },
];
